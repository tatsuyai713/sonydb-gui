# Permit the active desktop user to issue SonyDb's device-ID SCSI query to a
# mounted Sony USB storage device. Filesystem permissions still control writes.
SUBSYSTEM=="block", ENV{ID_BUS}=="usb", ENV{ID_VENDOR_ID}=="054c", TAG+="uaccess"
